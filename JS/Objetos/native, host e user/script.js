// Construtores de objetos nativos
Object;
String;
Array;
Function;

// Objetos do browser
NodeList;
HTMLCollection;
Element;

//User
const Pessoa = {
  nome: "André",
};

//Verificar se existe
if (typeof Array.from !== "undefined") 
if (typeof NodeList !== "undefined");
